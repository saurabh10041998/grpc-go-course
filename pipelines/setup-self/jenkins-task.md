# task
- jenkins should run the job in two sequential stage 'install' and 'verify'

- for install stage
   - install following packages 
        - sshpass using apt package manager
        - python3 and pip3 using apt package manager
        - ansible using pip

- for verify stage
    - check all above packages are installed properly